{
	"name": "frontend",
	"version": "1",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite dev --clearScreen false --host",
		"build": "vite build",
		"preview": "vite preview",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch"
	},
	"devDependencies": {},
	"dependencies": {}
}
