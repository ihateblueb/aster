class NotificationService {
	public get(where: object) {}
	public getMany(where: object) {}
	public create(to: string, from: string, type: string, note?: string, user?: string) {}
}

export default new NotificationService();
